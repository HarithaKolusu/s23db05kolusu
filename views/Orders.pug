extends layout

block content
  h1= title
  - let items = results
  div(class="container-fluid")
  div(class="row")
   each o,index  in results
    div(class="col-sm-2" onclick =`buy(${index})`) #{o.name}, #{o.id}, #{o.price}

     script.
        function buy(a)
        {
            alert(`You purchased Item ${a+1}`)
        }